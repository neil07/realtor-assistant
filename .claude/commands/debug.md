# /debug — 排查问题

用户会描述遇到的问题，按以下流程排查：

1. 读取相关脚本代码，理解上下文
2. 检查 `skills/listing-video/output/` 下最近的 `run.log` 和 `run.jsonl`
3. 定位问题：
   - API 调用失败？→ 检查 .env 配置 + API 响应
   - ffmpeg 错误？→ 检查输入文件格式 + 命令参数
   - 视频质量问题？→ 检查 prompt 模板 + 模型参数
   - 流程中断？→ 检查状态机状态 + 上下文数据
4. 给出修复方案并执行
